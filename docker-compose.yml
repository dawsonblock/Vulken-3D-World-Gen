version: "3.9"

services:
  engine:
    build: .
    depends_on: [redis]
  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
    command: ["redis-server","--appendonly","yes"]
    volumes: [ "redisdata:/data" ]
volumes:
  redisdata:

